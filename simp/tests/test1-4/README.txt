Files and subdirectories in this directory are structures NOT in the way that the scripts need in order to run!
Some pathings are broken. 

If one wants to run the scripts, move a certain script ending with '.py' to a parent folder tests/ and also move 'yolo_dataset' to the same tests/ directory.
